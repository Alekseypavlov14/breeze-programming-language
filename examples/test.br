import { console } from "@std/console.br"
import { number } from "@std/types.br"

const command = number(console.input("Insert command: "))

if (command > 5) {
  console.output("Inserted number bigger than 5")
} else {
  console.output("Inserted number is smaller than or equals to 5")
}
