function output(message, end = "\n") {
  _builtin_console_output(message + end)
}

function input(prompt) {
  return _builtin_console_input(prompt)
}

export const console = {
  output: output,
  input: input,
}
